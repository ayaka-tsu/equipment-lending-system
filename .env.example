# ローカル開発用の設定。`cp .env.example .env` でコピーして使う。
# .env はコミットしない（NFR-S-14）。

# compose.yaml の PostgreSQL に接続する
DATABASE_URL=postgresql://app:app@localhost:5433/equipment_lending

# 更新系 API の Origin 検証に使う（NFR-S-11）
APP_ORIGIN=http://localhost:3000
