# 開発の進め方

このリポジトリでの作業ルールです（NFR-M-04〜M-09）。迷ったらこの文書に従ってください。

## 1. 1つの Issue を終わらせるまでの流れ

```
Issue を受け取る
  ↓ 仕様書を読む（docs/）
ブランチを切る
  ↓ 実装する（こまめにコミット）
ローカルでチェックを通す
  ↓
Pull Request を出す
  ↓ レビュー → 修正 → 再レビュー
承認されたらマージ（リーダーが行う）
```

### 1-0. ボードで状況を共有する

担当している Issue の状況は、[開発ボード](https://github.com/users/hata-tomoyuki/projects/2)のカードを動かして共有します。

| 列           | いつ動かすか                     | 自動／手動 |
| ------------ | -------------------------------- | ---------- |
| 未着手       | Issue を追加したとき             | 自動       |
| 着手中       | ブランチを切って実装を始めたとき | 手動       |
| レビュー待ち | Pull Request を出したとき        | 手動       |
| 修正中       | レビューの指摘を受け取ったとき   | 手動       |
| 完了         | PR がマージされたとき            | 自動       |

カードは Issue ページの右側「Projects」からも動かせます。

### 1-1. 着手する前に

1. Issue に書かれた **対象の仕様**（要件ID・画面ID・API）をすべて読む。
2. 読んでも判断できないことがあれば、**実装を始める前に** Issue にコメントで質問する。自分の判断で仕様を変えない。
3. Issue を自分にアサインする（GitHub の Assignees）。

### 1-2. ブランチを切る

`main` から切ります。**`main` に直接コミット・push はしません。**

```bash
git switch main
git pull
git switch -c feature/12-equipment-list
```

ブランチ名は `種別/Issue番号-内容` の形にします。

| 種別       | 使う場面           | 例                             |
| ---------- | ------------------ | ------------------------------ |
| `feature/` | 機能の実装         | `feature/12-equipment-list`    |
| `fix/`     | 不具合の修正       | `fix/31-overdue-timezone`      |
| `docs/`    | 仕様書・READMEのみ | `docs/8-setup-troubleshooting` |
| `chore/`   | 設定・雑務         | `chore/5-add-ci-cache`         |

### 1-3. コミットする

- **意味のあるまとまりごとに**コミットします。1日の終わりにまとめて1つ、はやめてください。レビューが読みにくくなります。
- メッセージは「何をしたか」を日本語で1行。迷ったら `種別: 内容` の形にします。

```
feat: 備品一覧APIに貸出可能数を追加
fix: 延滞の判定を日本時間で行うよう修正
docs: READMEにWSLの注意点を追記
test: 同時申請のテストを追加
```

コミットすると、ステージした変更に ESLint と Prettier が自動でかかります（husky + lint-staged）。

### 1-4. Pull Request を出す前に

ローカルで次を通します。`git push` すると型チェックとテストが自動で走りますが、先に自分で確認してください。

```bash
npm run lint
npm run typecheck
npm test
```

そのうえで、**自分の PR を自分でレビューします。**

- 差分を上から読み直す（GitHub の Files changed で読むと気付きやすい）
- デバッグ用の `console.log` やコメントアウトしたコードが残っていないか
- Issue の受入条件を1つずつ画面で試したか

### 1-5. Pull Request を出す

- テンプレートの項目をすべて埋めます。**「動作確認したこと」は、実際に試した手順と結果を書きます。**
- 画面を変更した場合はスクリーンショットを貼ります。
- 1つの PR には1つの Issue 分だけを入れます。ついでの修正は別の PR にしてください。
- まだ相談したい段階なら Draft で出して構いません。その旨をコメントに書いてください。

### 1-6. レビューを受ける

- 指摘には、直した場合も直さなかった場合も**必ず返信**します（「直しました」「ここはこういう理由でこのままにしました」）。
- 修正は同じブランチに追加でコミットして push します。force push はしないでください（レビュアーが差分を追えなくなります）。
- 指摘の意味が分からないときは、分かったふりをせずに聞いてください。
- 再レビューしてほしくなったら、PR にコメントで伝えます。

### 1-7. マージ

- 承認され、CI が成功したら**リーダーがマージ**します。
- マージ後、ブランチは削除します。

## 2. コードを書くときの約束

| 内容                                                                 | 根拠             |
| -------------------------------------------------------------------- | ---------------- |
| 権限チェックはサーバー側で行う。ボタンを隠すのは権限チェックではない | NFR-S-06         |
| 入力チェックはサーバー側でも行う                                     | NFR-S-08         |
| 日付の判定は日本時間で行う                                           | BR-18 / NFR-E-04 |
| 一覧は DB 側でページングする                                         | NFR-P-04         |
| 業務ルール（状態遷移・在庫・延滞）には単体テストを書く               | NFR-M-10・M-11   |
| DB のスキーマ変更はマイグレーションファイルで行う                    | NFR-M-08         |
| 色・余白は `globals.css` のデザイントークンを使い、色を直接書かない  | Figma のトークン |
| 秘密情報は環境変数。`.env` はコミットしない                          | NFR-S-14         |

## 3. ラベル

| ラベル            | 意味                                         |
| ----------------- | -------------------------------------------- |
| `type:feature`    | 機能の実装                                   |
| `type:bug`        | 不具合                                       |
| `type:question`   | 仕様確認                                     |
| `priority:must`   | リリースに必須                               |
| `priority:should` | できる限り入れる（時間が足りなければ後回し） |
| `area:ui`         | 画面                                         |
| `area:api`        | API・サーバー側                              |
| `area:db`         | DB・マイグレーション                         |

## 4. 困ったとき

- 環境構築でつまずいた → [README](README.md) の「7. 困ったとき」
- 仕様が分からない → Issue にコメント、または「仕様確認」の Issue を立てる
- 30分考えても分からない → 抱え込まずに聞いてください。何を試して何が起きたかを書いてもらえると早いです。
